import React from "react";

export const StepsWrapper = ({ children }) => {
  return (
    <div
      className="
    h-full
    flex
    justify-center
    items-start
    "
    >
      {children}
    </div>
  );
};
